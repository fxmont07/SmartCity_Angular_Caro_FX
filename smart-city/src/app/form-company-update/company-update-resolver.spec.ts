import { CompanyUpdateResolver } from './company-update-resolver';

describe('CompanyUpdateResolver', () => {
  it('should create an instance', () => {
    expect(new CompanyUpdateResolver()).toBeTruthy();
  });
});
