import { StudentUpdateResolver } from './student-update-resolver';

describe('StudentUpdateResolver', () => {
  it('should create an instance', () => {
    expect(new StudentUpdateResolver()).toBeTruthy();
  });
});
